package org.skyweave.service.api.utils.enums;

// Enum for saved work status
public enum SavedWorkStatus {
  ACTIVE, // Currently saved
  PURCHASED, // Work has been purchased
  ARCHIVED // Removed from active saves
}
